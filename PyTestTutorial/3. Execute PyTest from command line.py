# i if we want to run all test in module we should go to needed directory and run "pytest" command
# also it will run test in child folders
# if we want more details : pytest -v (will provide name of each test)
# pytest -h - will show of abilities
# pytest -s will show print statements
# If we want run particular module: pytest <full file name> -flags
# If we want run particular tests: -k <letters> - it will run tests which have letters in name

